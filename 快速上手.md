# 司印云打印 MCP · 全新环境快速上手

> 面向一个全新的 WorkBuddy（或新电脑、新客户环境），从零开始把这套司印云打印 MCP 跑起来。

## 0. 前置条件

在开始前，请先确认你手上有这些信息（司印打印系统部署方提供）：

- 司印服务器 **IP** 和 **端口**（默认 8110）
- **solutionkey**（司印分配的解决方案密钥）
- **打印机队列 ID**
- **登录账号**

并且司印管理后台已开启**免密打印**：
> 司印管理后台 → 云打印管理 → 移动打印 → 选择模块 → 通过SDK打印 → 「不校验密码」选择「是」

本机需装有 **Python 3.10 及以上**。

## 1. 拿到项目文件

把 `司印云打印MCP` 整个文件夹拷到本机任意位置（例如 `D:\司印云打印MCP`）。

> 注意：分发给客户时，只需给 `.env.example`，**不要给 `.env`**（那是别人环境的内网参数）。

## 2. 安装依赖

打开终端（PowerShell / CMD），进入项目目录：

```bash
cd D:\司印云打印MCP

# 创建虚拟环境
python -m venv .venv

# 激活（Windows）
.venv\Scripts\activate

# 安装依赖
pip install -r requirements.txt
```

> 如果 `pip` 下载慢或卡住，加清华镜像：
> `pip install -r requirements.txt -i https://pypi.tuna.tsinghua.edu.cn/simple`

## 3. 配置参数（每个司印环境不同）

复制模板并填写：

```bash
# Windows
copy .env.example .env
# macOS / Linux
cp .env.example .env
```

用编辑器打开 `.env`，逐项改成你自己司印的参数：

```
SIYIN_HOST=你的司印服务器IP
SIYIN_PORT=8110
SIYIN_SOLUTIONKEY=你的solutionkey
SIYIN_PRINTER_QUEUE=你的队列ID
SIYIN_LOGIN_ACCOUNT=你的账号
```

## 4. 在 WorkBuddy 里配置 MCP

WorkBuddy 的 MCP 配置存在这个文件里：

```
C:\Users\<你的用户名>\.workbuddy\mcp.json
```

打开它（没有就新建），加入：

```json
{
  "mcpServers": {
    "siyin-cloud-print": {
      "command": "D:\\司印云打印MCP\\.venv\\Scripts\\python.exe",
      "args": ["D:\\司印云打印MCP\\server.py"]
    }
  }
}
```

> 关键点：
> - `command` 指向第 2 步创建的 `.venv` 里的 `python.exe`
> - `args` 指向 `server.py` 的**绝对路径**
> - 参数不在这个文件里，都在 `.env` 里（这个文件不支持注释，所以参数放 `.env`）

## 5. 在 WorkBuddy 里信任并启用

1. 打开 WorkBuddy 的**连接器管理**页面
2. 右上角「自定义连接器」入口，找到刚加的 `siyin-cloud-print`
3. 点击**信任**，启用它

## 6. 开始使用

回到对话，直接用自然语言说，例如：

- 「把 `D:\文件.docx` 上传到司印打印，彩色 A4 3 份双面」
- 「打印 XX 文件，黑白 2 份」
- 「司印服务器通不通？」

WorkBuddy 会自动调用 MCP 的 `upload_document` / `check_server` 工具，返回 jobId 即上传成功。

## 常见问题

| 问题 | 处理 |
|------|------|
| `连接失败：无法连接到 ...` | ping 一下司印服务器 IP，确认端口有没有监听、司印服务是否在跑 |
| `FAIL:NoLicense` | 司印系统许可证过期/未激活，联系司印厂商续期 |
| `.env` 改了不生效 | 重启 WorkBuddy 或重新启用连接器 |
| 想改打印默认参数 | 直接在对话里说（如「黑白」「5份」「A3」），不用改文件 |
